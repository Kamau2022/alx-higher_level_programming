#!/usr/bin/python3
def number_keys(a_dictionary):
    """ function that returns the number of keys in a dictionary
    Args:
      a_dictionary: number of keys
    Return: the number of keys in a dictionary
    """
    k = a_dictionary.keys()
    b = len(k)
    return b
